{
  "name": "nova-english",
  "version": "1.0.0",
  "description": "Nova English o'quv platformasi - Backend, Student App va Admin Panel",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,STUDENT,ADMIN\" --prefix-colors \"cyan,green,yellow\" \"npm run dev:backend\" \"npm run dev:student\" \"npm run dev:admin\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:student": "npm run dev --prefix student-app",
    "dev:admin": "npm run dev --prefix admin-panel",
    "tunnel": "node tunnel.js",
    "install:all": "npm install --prefix backend && npm install --prefix student-app && npm install --prefix admin-panel"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
